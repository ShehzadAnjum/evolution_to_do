apiVersion: v1
kind: ConfigMap
metadata:
  name: evolution-todo-config
  namespace: evolution-todo
  labels:
    app.kubernetes.io/name: evolution-todo
data:
  # Backend configuration
  BETTER_AUTH_URL: "http://backend:8000"
  CORS_ORIGINS: "http://frontend:3000,http://localhost:3000"
  
  # Frontend configuration
  NEXT_PUBLIC_API_URL: "http://backend:8000"
