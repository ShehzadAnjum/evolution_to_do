# NOTE: In production, use Kubernetes secrets management (e.g., sealed-secrets, external-secrets)
# This file is a template - DO NOT commit with real values
apiVersion: v1
kind: Secret
metadata:
  name: evolution-todo-secrets
  namespace: evolution-todo
  labels:
    app.kubernetes.io/name: evolution-todo
type: Opaque
stringData:
  # Replace these with actual values (or use external secrets manager)
  DATABASE_URL: "postgresql://user:password@host/database?sslmode=require"
  BETTER_AUTH_SECRET: "your-32-character-secret-key-here"
  OPENAI_API_KEY: "sk-your-openai-api-key"
